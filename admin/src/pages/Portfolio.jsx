import DataTable from '../components/DataTable';

const Portfolio = () => <DataTable title="Portfolio Management" columns={['Title', 'Category', 'Published']} rows={[]} />;

export default Portfolio;
